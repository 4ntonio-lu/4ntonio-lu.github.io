<<<<<<< Updated upstream
name: Deploy to GitHub Pages
=======
name: Build & Deploy to Pages

>>>>>>> Stashed changes
on:
  push:
    branches: [main]

<<<<<<< Updated upstream
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Build project
        run: npm run build
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: main
          enable_jekyll: false
          cname: 4ntonio-lu.github.io
=======
permissions:
  contents: write
  pages: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Build site
        run: npm run build

      - name: Clear root & copy build
        run: |
          shopt -s extglob
          rm -rf !(.git|.github)            # wipe out old source
          cp -R dist/* .                   # move built files to root

      - name: Commit & push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Deploy to GitHub Pages" || echo "No changes"
          git push origin main
>>>>>>> Stashed changes
